# --- Site Info ---
SITE_NAME=""
SITE_TAGLINE=""
CONTACT_EMAIL="hello@example.com"       # for displaying a mailto link; not private
TIMEZONE="Asia/Kuching"

# --- Features / Toggles (non-secret) ---
DEFAULT_THEME="system"                  # system | light | dark
ENABLE_SEARCH="true"
ENABLE_NEWSLETTER="false"
SHOW_META_REALM="true"
SHOW_CREATIVE_REALM="true"

# --- Routing / Build (non-secret helpers) ---
USE_HASH_ROUTER="true"                  # true is safest for GitHub Pages
BASE_PATH=""                            # leave empty; GH Actions sets correct base-href

THEME_PALETTE=yin   # metal|earth|wood|fire|water| yang / abyss / lunar / storm / natural / minimal / mono
THEME_MODE=system     # system|light|dark


# dart run tools/gen_canary.dart "password"
AUTH_CANARY_SALT=
AUTH_CANARY_NONCE=
AUTH_CANARY_DATA=
AUTH_CANARY_MAC=
